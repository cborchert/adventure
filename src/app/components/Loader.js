import * as PIXI from "pixi.js";
import constants from "../constants";

export default key => {
  /*
  const { SCENE } = constants;
  const { texture } = PIXI.Loader.shared.resources[key];
  const { width, height } = texture;
  const sprite = new PIXI.TilingSprite(texture, width, height);
  sprite.x = sprite.y = 0;
  sprite.tilePosition.x = sprite.tilePosition.y;
  sprite.scale.x = sprite.scale.y = SCENE.height / height;
  sprite.width = SCENE.width;
  return sprite;

  const image = PIXI.Sprite.from('https://crossorigin.me/https://s3-us-west-2.amazonaws.com/s.cdpn.io/107366/img_landscape.jpg');

// center the sprite anchor point
hawaii.anchor.x = 0;
hawaii.anchor.y = 0;

// move the sprite to the center of the canvas
hawaii.position.x = 200;
hawaii.position.y = 200;

return Image;*/
};
